apiVersion: apps/v1
kind: Deployment
metadata:
  name: bodhi-user-system
  namespace: apps
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: bodhi-user-system
  template:
    metadata:
      labels:
        app: bodhi-user-system
    spec:
      nodeSelector:
        nodegroup: "int"
        topology.kubernetes.io/zone: "ap-southeast-1a"
      containers:
        - name: bodhi-user-system
          ports:
            - name: supplement
              protocol: TCP
              containerPort: 5433
          livenessProbe:
            httpGet:
              path: /
              port: 5433
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 5433
            initialDelaySeconds: 5
            periodSeconds: 10
          image: 476114114107.dkr.ecr.ap-southeast-1.amazonaws.com/bodhi-user-system:0.0.6
          env:
            - name: DATABASE_URL
              value: "postgres://bodhiint:bodhiint@a0b3e412b8bcd4995aa03b0d4ce4c8c8-581959458.ap-southeast-1.elb.amazonaws.com:5432/app?sslmode=disable"
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 200m
              memory: 200Mi
---
apiVersion: v1
kind: Service
metadata:
  name: bodhi-user-system
  namespace: apps
spec:
  type: LoadBalancer
  ports:
    - port: 5433
      targetPort: 5433
  selector:
    app: bodhi-user-system
  